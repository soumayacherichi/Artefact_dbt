sources:
  - name: JAFFLE_SHOP
    database: RAW
    schema: JAFFLE_SHOP
    config:
      freshness:
        warn_after:
          count: 12
          period: hour
        error_after:
          count: 1
          period: day
          loaded_at_field: _etl_loaded_at
    tables:
      - name: CUSTOMERS
        freshness: null
      - name: ORDERS